# Install

!!! note "Draft"
    This page is a placeholder. Content is tracked in ticket `td-w0sm`.

```bash
pip install -e .
pip install -e ".[dev]"
pip install -e ".[analysis]"
pip install -e ".[lsp]"
```

Python: `>=3.10`
